#ifndef GEOMETRY_H
#define GEOMETRY_H

#include "led_types.h"
#include "led.h"

pattern_t draw_square(const uint8_t side_length);
pattern_t draw_rect(const uint8_t width, const uint8_t height);

#endif
